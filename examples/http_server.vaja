let server = Http.createServer()
let handler = fn(req)
  print("CALLED")
  print(req)
  return {
    "status": 200,
    "body": "HELLO"
  }
end
server |> Http.addRoutes([["/", handler]]) |> Http.listen(8080)
