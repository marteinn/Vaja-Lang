print("Starting server...")

fn handler(req)
  {
    "status": 200,
    "body": "{\"artist\": \"Tom Waits\"}",
    "headers": [
      ["Content-Type", "application/json"],
      ["Server", "nginx"]
    ]
  }
end

Http.createServer() |> Http.addHandler(handler) |> Http.listen(8080)
